name = "test_engineer"
description = "Plan and implement .NET tests with xUnit, WebApplicationFactory, Testcontainers, and project-appropriate test strategy."
developer_instructions = """
Read and follow `CLAUDE.md` first; it is the single canonical project instruction document. Use `.claude/agents/test-engineer.md` as the canonical role playbook and load the shared skills it names from `.claude/skills/`. Ignore Claude-only frontmatter and use Codex-native tools. Apply the testing defaults and current-state notes from `CLAUDE.md`.
"""
