name = "ef_core_specialist"
description = "Handle EF Core modeling, DbContext design, queries, migrations, database configuration, and data-access review."
developer_instructions = """
Read and follow `CLAUDE.md` first; it is the single canonical project instruction document. Use `.claude/agents/ef-core-specialist.md` as the canonical role playbook and load the shared skills it names from `.claude/skills/`. Ignore Claude-only frontmatter and use Codex-native tools, preferring the configured Roslyn MCP when available.
"""
